## 说明

1. 由于近期课业较繁忙，目前只写了个框架，部分样式和插图还没有放入，答辩前如果一有时间我就会尽量补充，非常非常抱歉。。。

2. 首页的番剧榜单由于分类过多且框架样式雷同所以只插入了一个，番剧列表同理只插入了一行，后续思路是将相同框架打包起来用js批量插入。

3. 新番时间表那里的渐变色背景和时间轴的插入方式感觉有点复杂就暂时放着没做，毕竟有超出父容器的部分，直接用绝对定位加负值还是用伪元素？或者其他方法？有待尝试。